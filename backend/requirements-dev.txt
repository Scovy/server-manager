# Homelab Dashboard — Backend Dev Dependencies
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.1

# Linting & formatting
ruff==0.11.3

# Type checking
mypy==1.15.0

# HTTP test client (also a prod dep, but listing explicitly for clarity)
httpx==0.28.1
